package com.whut.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.whut.smartinspection.model.OpenDoorRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "OPEN_DOOR_RECORD".
*/
public class OpenDoorRecordDao extends AbstractDao<OpenDoorRecord, Long> {

    public static final String TABLENAME = "OPEN_DOOR_RECORD";

    /**
     * Properties of entity OpenDoorRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property DeviceName = new Property(1, String.class, "deviceName", false, "DEVICE_NAME");
        public final static Property UnlockingDate = new Property(2, String.class, "unlockingDate", false, "UNLOCKING_DATE");
        public final static Property LockingDate = new Property(3, String.class, "lockingDate", false, "LOCKING_DATE");
    }


    public OpenDoorRecordDao(DaoConfig config) {
        super(config);
    }
    
    public OpenDoorRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"OPEN_DOOR_RECORD\" (" + //
                "\"id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"DEVICE_NAME\" TEXT," + // 1: deviceName
                "\"UNLOCKING_DATE\" TEXT," + // 2: unlockingDate
                "\"LOCKING_DATE\" TEXT);"); // 3: lockingDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"OPEN_DOOR_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, OpenDoorRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(2, deviceName);
        }
 
        String unlockingDate = entity.getUnlockingDate();
        if (unlockingDate != null) {
            stmt.bindString(3, unlockingDate);
        }
 
        String lockingDate = entity.getLockingDate();
        if (lockingDate != null) {
            stmt.bindString(4, lockingDate);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, OpenDoorRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(2, deviceName);
        }
 
        String unlockingDate = entity.getUnlockingDate();
        if (unlockingDate != null) {
            stmt.bindString(3, unlockingDate);
        }
 
        String lockingDate = entity.getLockingDate();
        if (lockingDate != null) {
            stmt.bindString(4, lockingDate);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public OpenDoorRecord readEntity(Cursor cursor, int offset) {
        OpenDoorRecord entity = new OpenDoorRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // deviceName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // unlockingDate
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // lockingDate
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, OpenDoorRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDeviceName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setUnlockingDate(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setLockingDate(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(OpenDoorRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(OpenDoorRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(OpenDoorRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
